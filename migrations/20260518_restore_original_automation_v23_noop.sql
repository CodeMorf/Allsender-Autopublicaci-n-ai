-- V23 RESTORE ORIGINAL AUTOMATION
-- No ejecuta cambios. La restauración del flujo original se hace desde Git o desde un ZIP/base anterior del servidor.
SELECT 'V23 restore original automation: no SQL changes required' AS status;
